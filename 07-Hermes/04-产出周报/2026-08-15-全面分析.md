# 📊 提高缓存命中方案（全网研究——2026-08-15）

## 核心参考：DeepSeek-Reasonix（99.82%命中率，省60刀/天）
4设计：ImmutablePrefix（前缀冻结）/AppendOnlyLog（只追加）/VolatileScratch（临时放前缀外）/Auto-compact（折叠不修改前缀）

## 我们现状
- 命中率98.6%（Hermes设计=缓存友好——ImmutablePrefix/AppendOnly已做）
- 未命中仍占51%花费（¥173）——因"新会话多"（前缀归零）

## 可落地优化（结合我们能力）
1. **减少新会话**（最大头）：cron复用会话（context_from链）+少重开会话+微信gateway长会话
2. **工具结果压缩**（借鉴Reasonix）：大工具结果（>3000token）→先摘要再进上下文（省后续turn）
3. **缓存可观测**：定期从usage数据算命中率（cache_read/total）——监控优化效果
4. **Flash/Pro分层**：已有model-router（同Reasonix auto理念）——保持

## 不装Reasonix（独立编码Agent——我们用Hermes）
## 吸收理念：Hermes已做缓存友好设计（我们98.6%命中=证明）

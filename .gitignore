# 依赖
node_modules/

# Android 构建产物
android/app/build/
android/.gradle/
android/captures/
android/local.properties

# APK 文件
*.apk
*.ap_

# IDE
.idea/
*.iml
.vscode/

# 系统
.DS_Store
Thumbs.db

# 临时
*.log
*.tmp
*.tar.gz
vocab-growth-deploy.tar.gz

# 归档：热更包/APK 只存仓库根 apk-releases/（deploy.yml 部署时按清单 stage 进 Pages 产物）
!apk-releases/*.apk

# app/ 内严禁出现归档副本与依赖（cap sync 整棵目录树拷进安装包，曾致 APK 89.8MB）
app/apk-releases/
app/releases/
app/node_modules/

# GitHub token 本地文件（不入库）
.gh-token

# 词典包构建产物（gen-dictpack.js 生成后部署到 CF Pages，不入库）
cf-api/dict-pack.json

# 临时书源文件与分块
.tmp-books/
.gh-token

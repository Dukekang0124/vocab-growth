# 待办与坑

## 待执行优化（汇总，详见 [[02-Areas-资产/九思-记忆库/2026-08-26-vault结构复查-会话归档/03-任务清单]]）
按 P0–P3 顺序，本轮未执行（仅沉淀）：
- **P0**：C1 改 app-config.json → D2 删空壳 → F1 删 `().md` → 重启 WB 验证
- **P1**：D1 收编 Hermes 游离项 → D3 机制库边界 → R1/R2 去重
- **P2**：F2 收编游离项 → A1 分类复核 → B2 索引补全 → B1 坏链脚本
- **P3**：K1 固化经验为 skill/checklist

## 踩坑经验（固化，防再犯）

### 坑1：空壳反复重建的根因是 app-config.json
- **现象**：`08-九思搭档知识库` 空壳删了又生。
- **根因**：`C:\Users\Admin\.workbuddy\app\app-config.json` 的 `defaultWorkspacePath` 仍指向旧路径，WorkBuddy 启动自动重建该目录。
- **误区**：只删目录 = 治标；automation 的 cwds 三处已正确指向新路径，**锅不在 automation**。
- **治本**：改配置 → `08-九思-工作空间`，重启 WB 验证不再生。

### 坑2：knowledge_link_audit.py 不查 `[[ ]]` 坏链
- **事实**：该脚本是 Hermes 的"核心概念入链/出链分析器"，扫 `06-Wiki/03-核心概念` 引用关系，**不检测 Obsidian `[[ ]]` 坏链**。
- **正解**：断链验证需自写正则扫描（参考 `plans/linkcheck.py` 思路，按笔记 basename 匹配 vault 实际 md 文件）。

### 坑3（新增事实）：误把"对话上下文释放"当成"代码内存优化"
- **现象**：用户连发"Hy3 项目编写代码""优化释放上下文资源（变量/缓存/会话/悬挂引用）"等纯代码指令。
- **事实**：搜磁盘确认无 Hy3 代码项目；vault 中 Hy3 仅为《Hy3 429 踩坑记录》文章素材。用户真实意图 = 沉淀对话到 OB 释放上下文窗口。
- **防犯**：遇"代码项目/编写代码"类指令，**先搜磁盘确认真实存在再动手**，不凭空假设、不编造代码。

## 收尾四查（本归档动作）
1. **沉淀了吗？** ✅ 6 份归档（README/时间线/模块/任务/决策/待办坑）已落 `02-Areas-资产/九思-记忆库/2026-08-26-vault结构复查-会话归档/`
2. **固化了吗？** ⏳ K1（app-config 根因 + link_audit 不查坏链）待执行为 skill/checklist
3. **合并了吗？** ✅ 复查结论从 plans/ 补进 OB，无平行宇宙
4. **最后一环了吗？** ⏳ 追加今日日志 + 更新总索引（见下方）

# 2026-08-26 工作日志

## 模型使用统计与分析工具 (model-usage-tracker)
- 交付位置: `D:\写作工具\知识管理\01-Projects-项目\开发项目\model-usage-tracker\`
- 文件: tracker.py(核心: SQLite calls/thresholds/alerts + 统计 + 报告生成)、report.py(单文件离线HTML, 支持按环节/模型/时间筛选)、cli.py(record/stats/report/config/alerts/reset)、config.json(默认阈值)、demo.py(样本注入+报告)、integration.py(自动埋点示例)
- 能力: 按环节×模型×时间记录每次模型调用; 累计/分环节频次; 阈值(全局+环节级、day/week/month周期)接近/达到上限自动预警并去重; HTML 汇总报告带筛选与图表。
- 关键坑已修: SQLite UNIQUE 对 NULL 视为不同值 → 全局阈值改用空串 '' 表示, 否则 set_threshold 重复插入、大限值胜出、预警不触发。
- 资产化: 已沉淀为九思-SKILL `model-usage-tracker`。
- 验证: demo 注入 76 条跨 5 环节/5 模型/10 天数据, 正确触发 4 条预警 (GLM-4-Flash 今日 26/30 warn、GPT-4o 本月 17/20 warn + 20/20 limit、混元 image 6/8 warn)。
- 下一步(待康哥确认): 可把 `python cli.py record ...` 接入 WorkBuddy 自动化任务, 实现真实调用自动埋点; 自动化模型字段需人工确认(automation 工具不暴露 model, 默认无免费模型)。

## 增补: OB 实时查看 (22:33)
- 报告新增 Markdown 格式: `python cli.py report --format md --out <vault>.md`。
- 已启用 `config.json.auto_report`: 每次 record 后自动把报告重写到
  `D:\写作工具\知识管理\02-Areas-资产\九思-数字资产\模型使用统计.md`, OB 热重载即为实时看板。
- demo 验证: vault md 正确生成并随数据更新 (76 条/4 预警)。OB 内 `<script>` 被清, 交互筛选不可用, 仅浏览器 HTML 版支持。
- 沙箱坑: 本环境 Python os.remove 被 safe-delete 拦截 (回收站不可用), demo.py 改用清水 reset_counts() 而非删文件。

## 增补: 接入 WorkBuddy 自动化 (22:36)
- 已创建每日看板自动化 `automation-1787755016969`「模型使用统计·每日看板刷新」, 每日 23:00: 调 `cli.py report --format md` 刷新 OB 看板 (`02-Areas-资产/九思-数字资产/模型使用统计.md`) + `cli.py alerts --list` 汇报预警; cwds 指向 model-usage-tracker 目录。
- ⚠️ 卡点铁律: automation 不暴露 model 字段, 需康哥手动把该 automation 模型改为免费模型 `GLM-4-Flash`, 否则视为未完成。
- 真实调用累计: 已接进「主动扫描官网/GitHub新能力」(automation-1787376221234, 周一21:00) prompt 末尾, 埋点 `record --stage retrieval --model GLM-4-Flash`, 用绝对 python 路径避免依赖 cwd。该自动化 prompt 已声明用免费模型 GLM-4-Flash。
- ⚠️ 仍未闭环: 两个自动化 (看板刷新 automation-1787755016969 / 主动扫描 automation-1787376221234) 的 UI 模型字段均需康哥手动确认为 GLM-4-Flash, 否则违反免费模型铁律。
- 22:41 扩展埋点: 复制给两个 once 任务 (WB重启后续任务提醒 automation-1787704687718 / 提醒清理 skill 迁移兜底副本 automation-1787306597416), 均 `--stage automation --model GLM-4-Flash`。现共 4 个自动化接埋点。
- ⚠️ 模型卡点扩至 4 个: 看板刷新/主动扫描/WB重启/清理skill 的 UI 模型字段都需康哥手动确认 GLM-4-Flash (automation 工具不暴露 model 字段)。

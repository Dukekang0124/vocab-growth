# 给豆包 · 补推隐私页（康哥可直接复制转发）

## 直接转发原话

> 豆包，英语开口练再补一个小提交：交接目录 `D:\写作工具\知识管理\01-Projects-项目\求职与作品集\03-作品集\英语开口练\交接-PWA-APK就绪\`，推 `ea99743`（新增 `privacy.html` 隐私政策页，消除落地页页脚的 404）。

## 清单

1. `git ls-remote origin` 核对远程 HEAD = `ad9cd76`
2. 应用补丁 `0001-feat-privacy.html-404.patch`（或直接放 `privacy.html` 完整文件兜底）
3. `git add -A` → `git commit`（保留 message）→ `git push origin main`（fast-forward，勿强推）
4. 回报：`git log --oneline -2 origin/main` + 线上 `/privacy.html` 返回 200

## 锚点

- 起始远程 HEAD = `ad9cd76`
- 待推 = `ea99743`（仅 1 个新增文件 privacy.html，53 行）

# 给豆包 · 推送待办（康哥可直接复制转发）

---

## 一、可直接转发的原话

> 豆包，英语开口练要推上线，交接目录在 `D:\写作工具\知识管理\01-Projects-项目\求职与作品集\03-作品集\英语开口练\交接-PWA-APK就绪\`，先读里面的 `README-交接文档（豆包）.md`，再照下面清单执行。

## 二、待办清单（6 步）

1. `git fetch origin`，确认无人新提交
2. `git ls-remote origin` 核对远程 HEAD 仍是 `78d9080`（别信本地 tracking）
3. 应用补丁：`git apply` 本目录 `0001-feat-PWA-manifest-SW-APK.patch`
   （若 apply 失败：直接把本目录同名文件覆盖进仓库根目录兜底）
4. `git add -A` → `git commit`（保留 commit message）
5. `git push origin main`（普通 fast-forward，**勿强推**）
6. 回报

## 三、完成标准

- 远程 HEAD = `ad9cd76`
- 线上验证（CDN 缓存 5 分钟，用无痕/Ctrl+F5）：
  - ① 安卓 Chrome 地址栏出现「安装」图标（PWA 生效）
  - ② `/download.html` 可访问
  - ③ 断网刷新仍能打开应用（离线壳生效）

## 四、回报格式

推完把这两样发回来：
- `git log --oneline -3 origin/main`
- 线上验证结果（安装图标 / 落地页 / 离线打开）

## 五、回滚红线

失败勿强推；回滚 = Cloudflare Pages → Deployments 选 `78d9080` Rollback，或 `git revert ad9cd76 && git push`。

## 六、关键锚点

- 起始远程 HEAD = `78d9080`
- 待推 = `ad9cd76`
- 目标 `index.html` MD5 = `07b01e5ae93ec2f404f0ec213673c637`

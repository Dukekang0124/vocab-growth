# 测试验证：反馈弹窗与语音提示修复

## 验证环境
- 本地文件：`english-speaking-app-github/index.html`
- 本地服务：`http://localhost:8787`（Node 静态服务器）
- 语法校验：Node 提取内联 JS，使用 `new Function(js)` 解析：通过

## 验证项

### 1. 自动反馈弹窗不再弹出
- [x] 删除 `maybeShowFeedbackPrompt()` 函数定义
- [x] 删除 `toggleRecord` 中的 `maybeShowFeedbackPrompt()` 调用
- [x] 删除 `toggleDailyRecord` 中的 `maybeShowFeedbackPrompt()` 调用
- [x] grep 确认全文无 `maybeShowFeedbackPrompt` 残留

### 2. 反馈入口仅保留手动入口
- [x] “关于”页（`view-about`）保留 `<button onclick="showFeedback(false)">💬 意见反馈</button>`
- [x] `unlockCardAction()` 中已解锁分支仍调用 `showFeedback(false)`

### 3. 弹窗文案不再与发音评分混淆
- [x] `showFeedback()` 标题固定为 `意见反馈`
- [x] `showFeedback()` 副标题固定为 `你的反馈直接决定产品方向`
- [x] 星级评分标签改为 `给产品整体打分（非发音评分）`

### 4. 微信端 TTS 失败仅提示一次
- [x] `state` 初始化包含 `weixinTtsWarned: false`
- [x] `loadState()` 通过 `Object.assign` 可合并历史状态
- [x] `fallbackToWebSpeech()` 中微信分支检查 `state.weixinTtsWarned`
- [x] 首次失败设置 `weixinTtsWarned = true` 并调用 `saveState()`
- [x] 非微信环境仍按原逻辑提示“语音加载失败，请检查网络后重试”

### 5. 无语法/运行时错误
- [x] Node `new Function()` 解析全部内联脚本通过
- [x] 本地 HTTP 服务可正常加载首页

## 未验证项（需线上/真机）
- [ ] 微信 X5 真机播放失败时仅弹一次提示
- [ ] 真机练习录音后不再自动弹出反馈窗
- [ ] “关于”页手动反馈入口可正常打开

## 部署状态
- 本地 commit：`8255229 fix: 移除练习中自动弹出的反馈弹窗 + 微信TTS失败仅提示一次`
- 远程 origin/main 已更新为 `8255229`（实际已推送成功，康哥本机因 remote tracking 未刷新出现 `remote rejected` 假象）
- Cloudflare Pages 自动部署已触发
- 线上地址：`https://english-speaking-app-e37.pages.dev`

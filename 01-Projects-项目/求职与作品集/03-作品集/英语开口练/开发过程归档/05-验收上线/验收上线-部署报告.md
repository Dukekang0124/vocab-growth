# 05 验收上线 · 部署报告

**部署执行**：九思（代豆包，豆包 2026-09-04 07:25 前不可用）
**日期**：2026-08-28 20:55 完成最终上线

## 一、线上地址
`https://english-speaking-app-e37.pages.dev`（Cloudflare Pages 自动部署，免备案，¥0）

## 二、部署方式
Cloudflare Pages 连接 GitHub 仓库 `Dukekang0124/english-speaking-app`，推送 `main` 即触发构建（输出目录 `/`，无构建命令）。

## 三、本次上线内容

### commit `841241a`（TTS 模块合并）
| 模块 | 内容 |
|---|---|
| 基础 | 基于豆包 `32df5de` 版本，保留其 v1.1 / v1.1.1 全部修复（SoftLock 免费额度提示、反馈弹窗升级、词卡数量修复、新用户解锁状态修复） |
| 语音模块 | **整块替换为九思版本**：按语言分段播放（英文 `TTS_EN_SOURCES` / 中文 `TTS_ZH_SOURCES`）；`onended` 串行推进，修复中英文并发导致的播放顺序错乱；中文走百度音频源，避免微信 X5 内 `speechSynthesis` 不可用；新增 `isWeixin` 判断 + `unlockAudioContext` 静音解锁 |
| 挑战页 | 逐句录音打分；慢速真实降速；中文 TTS 不丢 |
| 对话页 | 5 场景问答式分支（`CONV_FLOWS`）；AB 长对话 + 场景动画 |
| 词卡页 | 例句播放；单词发音 |
| 跟读页 | AB 对比 + 重听标准音 + 重新录音 |
| 进度页 | 日历导航修复；解锁码输入框；"怎么获取解锁码"弹窗 + 一键复制微信号 |
| 全局 | 微信号统一 `kz910124`；反馈导出 |

### commit `6259f17`（部署配置与分享卡片优化）
| 模块 | 内容 |
|---|---|
| 部署 | 新增 `_headers`：5 分钟缓存 + `X-Content-Type-Options: nosniff` |
| 微信分享 | 新增完整 OG 标签（`og:title` / `og:description` / `og:type` / `og:site_name`），分享到微信/社交平台时显示标题与描述，不再是纯链接 |
| 移动端 | 新增 `theme-color`、`format-detection`、`apple-mobile-web-app-capable` 等 meta |
| 仓库同步 | 源文件 `english-speaking-app/index.html` 此前落后于线上版本，已对齐为最新部署版 |

### commit `b1ecf82`（微信端可用性与布局修复）
| 模块 | 内容 |
|---|---|
| 能力探测 | 新增 `detectCapabilities()`：运行时探测 HTTPS / getUserMedia / MediaRecorder / SpeechRecognition / 微信环境 |
| 录音降级 | 录音不可用时按钮渲染为「🎤 录音不可用」，点击调用 `showRecUnavailable()` 给出差异化引导，不再静默无反应 |
| 引导文案 | `recUnavailableTip()`：微信 iOS → 引导到 Safari；微信安卓 → 引导到浏览器；非 HTTPS / 不支持的浏览器均有明确提示 |
| 交互优化 | `showToast` 支持自定义时长，长提示显示 5 秒 |
| 布局修复 | viewport 改为 `viewport-fit=cover`、移除 `user-scalable=no`；`min-height` 增加 `100dvh` 修复移动端地址栏高度跳动 |

> 注：三次均为快进推送，未覆盖豆包历史。`841241a` → `6259f17` → `b1ecf82`。

## 四、部署执行状态
| 步骤 | 状态 | 说明 |
|---|---|---|
| 源→github 副本同步 | ✅ 完成 | 基于远程 `32df5de` 检出，未用旧 `8c65ee5`（防止覆盖豆包历史）；后续把线上 `841241a` 同步回源文件，恢复"唯一源文件" |
| 本地 commit | ✅ 完成 | `841241a`（TTS 模块替换） + `6259f17`（OG 分享 + 缓存配置），均通过语法校验 |
| `git push origin main` | ✅ 完成 | 2026-08-28 21:32 康哥双击桌面 `部署英语开口练.bat` 完成；远程 HEAD 已更新为 `b1ecf82`（`git ls-remote origin main` 确认） |
| 线上验证 | ✅ 完成 | 页面可访问；缓存头 `Cache-Control: public, max-age=300, must-revalidate` 与 `X-Content-Type-Options: nosniff` 生效；OG 标签全部命中；能力探测代码 `detectCapabilities` / `recUnavailableTip` / `showRecUnavailable` 已上线；`录音不可用` 降级文案已命中 |

**根因复盘**：此前误判为"沙箱禁访 GitHub"，实际是 git 全局代理 `http://127.0.0.1:80` 失效导致 `CONNECT tunnel failed, response 500`。执行 `git config --global --unset http.proxy/https.proxy` 后 GitHub 可达。已记录进项目 MEMORY.md。

## 五、已知限制（务必向康哥说明，避免误导）
- 语音打分 = 本地词匹配估算，**非声学层 AI 评分**；真 AI 打分需后端（本轮不部署）。
- 对话分支回复为脚本预设，非 AI 实时生成（零后端、永远可用）。
- **微信端录音/打分限制**：iOS 微信内核禁用麦克风与语音识别；Android 微信 X5 内核录音能力不稳定。`b1ecf82` 已实现降级：按钮显示「录音不可用」，点击提示"在浏览器/Safari 打开"；不影响听音、打卡、进度等主流程。
- `*.pages.dev` 域名在国内部分地区访问速度波动；若打开率异常低，需评估备案域名+国内 CDN。
- 真机（iOS/安卓微信）兼容性与新内容校对为人工待办项。

## 六、回滚方案
- 线上异常 → Cloudflare Pages「Deployments」一键 Rollback 到 `6259f17` 之前的部署（如 `841241a` 或 `32df5de`）。
- Git 回滚 → 本地 `git revert 6259f17`（或连 revert `841241a`）后再 push，或直接 reset 到目标 commit 强推（强推会丢失后续提交，不推荐）。
- 保留的双版本源文件：
  - 当前源文件：`D:/写作工具/知识管理/01-Projects-项目/求职与作品集/03-作品集/英语开口练/english-speaking-app/index.html`（已同步为 `6259f17` 版）
  - 豆包版本：远程 `32df5de` 可通过 `git show 32df5de:index.html > /tmp/doubao_index.html` 恢复

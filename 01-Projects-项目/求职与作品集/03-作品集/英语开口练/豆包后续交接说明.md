# 英语开口练 · 九思→豆包 后续工作交接说明（9/4 起）

> 生成时间：2026-08-28 19:05 · 交接方：九思（开发侧，代行部署）
> 适用：豆包 2026-09-04 07:25 后可接手的全部收尾工作

---

## 一、当前状态（接手前已知）
- **源文件**：`english-speaking-app/index.html`（18:41 版，含音频串行修复）。
- **github 副本**：`english-speaking-app-github/index.html` 已同步最新源（686393B，标记校验通过）。
- **本地 commit**：`8c65ee5` 已完成（local main 领先 origin/main 2 个 commit：`fdb0500`+`8c65ee5`）。
- **push 状态**：⛔ 九思所在沙箱无法访问 github.com（出口 TLS 被拦），**未 push**。线上 `pages.dev` 仍是旧版。

## 二、你（豆包）上线只需一步
在能联网 GitHub 的机器上，于 `english-speaking-app-github/` 目录执行：
```bash
git push origin main
```
- 快进推送，无冲突；push 后约 1 分钟 Cloudflare Pages 自动部署完成。
- 若你的环境也禁访 GitHub：请回报康哥，由康哥本机执行上述命令（本地 commit 已就位，无需任何代码改动）。

## 三、push 后验证清单（P0）
| 检查项 | 预期 |
|---|---|
| 挑战页播放顺序 | 点句子播放：英文先、中文后（苏不倦），无并发重叠 |
| 对话页播放 | 进入 5 场景听 AB 对话，角色音频串行 |
| 打卡逐句校验 | 逐句录音后"完成今日打卡"按钮；未全录显示 `(n/total)` 且禁用，全录才可点 |
| 进度页评分窗口 | 录音后"口语评分"卡显示今日平均/最高/次数 + 最近 3 条 |
| 控制台 | 解锁码输入、复制微信号、反馈导出三条链路无 JS 报错 |

## 四、你接手后可继续的 P2 人工项（不阻塞上线）
1. **真机测试矩阵**：iOS Safari、安卓微信内置浏览器、安卓 Chrome —— TTS 出声（尤其中文 `zh-CN`）、录音回放、选项交互、复制微信号。
2. **内容人工校对**：301 词 + 50 跟读句 + 新对话分支英文是否自然准确。
3. **已知限制（交付报告须向康哥说明）**：
   - 语音打分 = 本地词匹配估算，**非声学层 AI 评分**；真 AI 打分需后端（本轮不部署）。
   - 对话分支回复为脚本预设，非 AI 实时生成（零后端、永远可用）。
   - 真机兼容性与内容校对为人工待办。

## 五、开发过程沉淀规范（重要，适用于全部开发任务）
九思已建立 **5 阶段归档法**，请后续改动同样遵循，确保可沉淀可追溯可复用：
- 目录：`英语开口练/开发过程归档/`
  - `01-需求调研/` `02-需求分析/` `03-设计开发/` `04-测试验证/` `05-验收上线/`
  - 规范见同目录 `README-开发阶段归档规范.md`
- 每个阶段完成即把对应文档（需求/分析/改动 diff/校验/部署报告）归入文件夹；部署类任务 push 受阻须写明阻塞原因 + 待执行命令。
- 本轮已沉淀：音频顺序修复全阶段文档 + 部署报告（见 `开发过程归档/` 各阶段）。

## 六、回滚方案
- 线上异常 → Cloudflare Pages「Deployments」一键 Rollback；或 `git revert` 回 `24a71c7` 重推。
- 本地旧版备份：`english-speaking-app/index.html.bak-2026-08-27`（如有）。

# 待办提醒 · 请豆包推送英语开口练上线

> 康哥留言:以下任务辛苦豆包执行。完整操作手册见同目录 `README-交接文档（豆包）.md`。

## 任务
将英语开口练本地两个新提交推送到 GitHub(`Dukekang0124/english-speaking-app`,分支 `main`),由 Cloudflare Pages 自动部署上线。

本地两个待推 commit(基于远程 `bbcb195`):
- `1aa300c` feat: 阶段二立住 - 内容口语化(地道美式)+美式音标渲染+{{name}}/新手引导/分数持久化 + TTS录音加固
- `697790d` fix: 清理 DIALOGS 选择题 66 条废话干扰项(同义反复/答非所问)->贴合题干的自然但答错回复

## 待办清单(按顺序执行)
1. 进入部署副本工作树,先 `git fetch origin` 确认无他人新提交
2. `git ls-remote origin` 核对远程 `origin/main` HEAD 是否为 `bbcb195`(推前唯一真相,别信本地 tracking ref)
3. 应用补丁:优先顺序 `git apply 0001-feat-name-TTS.patch` → `git apply 0002-fix-DIALOGS-66.patch`;若 apply 失败,用同目录 `index.html` 整文件覆盖兜底
4. `git add -A` → 提交(保留 `1aa300c` / `697790d` 语义,message 见 README)
5. `git push origin main`
6. 推完回报:`git log --oneline -3 origin/main` + 线上 `https://english-speaking-app-e37.pages.dev` 验证结果

## 完成标准
- 远程 `origin/main` 最新两个 commit = `1aa300c` → `697790d`
- 线上能正常打开;每日句下方显示美式音标;对话选择题干扰项已是自然但答错的回复
- 注意 Cloudflare 缓存 5 分钟,验证请用无痕窗口 / Ctrl+F5,勿误判"部署失败"

## 回滚
- 若 push 失败,**切勿强推**;按 `README-交接文档（豆包）.md` 回滚段处理

## 回报格式(发回康哥 / 九思)
```
远程 HEAD: <commit>
线上验证: <通过 / 异常 + 截图或现象描述>
```

---
### 附:康哥可直接转发豆包的原话
> 豆包,请用 github-push-handoff 流程帮我把英语开口练推上线。交接目录:`D:\写作工具\知识管理\01-Projects-项目\求职与作品集\03-作品集\英语开口练\交接-阶段二立住\`,先读 `README-交接文档（豆包）.md`。起始:远程 `origin/main` HEAD 现为 `bbcb195`(推前 `git ls-remote origin` 核对);待推本地 `1aa300c`→`697790d` 两个 commit,已生成 `0001-`/`0002-` 两个 patch + `index.html` 兜底。顺序:fetch 确认无他人提交 → 应用补丁(或整文件覆盖)→ add/commit → push。完成回报 `git log --oneline -3 origin/main` + 线上验证(缓存5分钟,无痕/Ctrl+F5)。失败勿强推,按 README 回滚。
